- Add (mock) support for Currency conversion, then modify forwarded to use it.
  Also configure connector.currency, and plugin.remote_currency.

- Add logging support.

- Add connector to list of forwarded-by when receiving a remote ILP Exception.

- Recheck Forwarder  MIN_MESSAGE_WINDOW constant and more in general Timeouts.
  How are them supposed to be used in "ILPv4". Is it needed at all?
